/**
 * @deprecated v4 — replaced by SystemPillars (3-column system cards in the
 * canonical Brand → Growth → Scale order). This stub only exists so that
 * older copies of the file in the repository are safely overwritten on
 * drag-and-drop uploads; it renders nothing and is not imported anywhere.
 */
export default function BentoFeatures() {
  return null;
}
